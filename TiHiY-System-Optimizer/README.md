# TiHiY System Optimizer

TiHiY System Optimizer — простий Windows-застосунок, який аналізує ПК, пояснює рекомендовані зміни людською мовою та застосовує лише те, що користувач підтвердив.

## v0.5

- адаптивний темний інтерфейс для звичайного користувача;
- аналіз CPU, GPU, RAM, Windows і плану живлення;
- безпечні рекомендації Windows з поясненням «що змінить»;
- пошук RSI Launcher, Star Citizen, OBS Studio, Discord і SteelSeries GG / Sonar;
- ручний вибір папки Star Citizen, якщо автопошук не спрацював; шлях перевіряється через `Bin64\StarCitizen.exe` і зберігається для наступних запусків;
- Star Citizen: LIVE / PTU / EPTU / TECH-PREVIEW, USER.cfg, мовна настройка, кількість активних параметрів і кеш шейдерів;
- сторінка «Очищення гри»: кеш шейдерів Star Citizen, тимчасові Cache / Code Cache / GPUCache RSI Launcher, Game.log / crash logs, а також необов’язкові глобальні DirectX та NVIDIA/AMD shader caches;
- `USER.cfg`, Controls/Mappings, профілі керування, `Data.p4k`, виконувані файли гри, Cookies/авторизація RSI Launcher, бібліотека ігор та маніфести не видаляються;
- окремий «netcode cache» не вигадується: DNS, Winsock та мережеві параметри Windows програма не скидає;
- кнопка відкриття RSI Launcher для офіційного Verify; TiHiY не імітує недокументований механізм Verify;
- сторінка «Стрім»: читання активного профілю OBS, полотна, вихідної роздільної здатності, FPS, енкодера, режиму виводу та частоти аудіо;
- визначення, чи запущені OBS, Discord, SteelSeries GG і процеси Sonar;
- нова сторінка «Автозапуск»: читає HKCU/HKLM Run та папки Startup, зрозуміло класифікує пункти й автоматично відмічає лише відомі необов’язкові користувацькі програми;
- вимкнення автозапуску не видаляє програми: реєстрові значення зберігаються для точного відновлення, а ярлики/файли Startup переносяться у резервну папку;
- кнопка «Повернути останні» відновлює останній набір вимкнених пунктів автозапуску;
- нова сторінка «NVIDIA / GPU»: назва GPU, версія й дата драйвера, VRAM, поточне навантаження, температура, використання VRAM і стан HAGS;
- живі показники NVIDIA читаються через локальний `nvidia-smi`, якщо він доступний; без нього сторінка використовує дані Windows і чесно показує, що живих метрик немає;
- Resizable BAR не визначається за непрямими ознаками BAR1 — якщо надійного способу немає, TiHiY не показує вигаданий результат;
- OBS / Discord / Sonar і NVIDIA у v0.5 аналізуються без автоматичного переписування їхніх конфігурацій;
- точна резервна копія попередніх значень реєстру й плану живлення;
- скасування останніх змін;
- self-contained single EXE для Windows x64.

## Безпека

Програма не змінює BIOS, PBO, Curve Optimizer або EXPO. Перед змінами Windows створюється резервна копія. Глибоке очищення блокується, якщо Star Citizen запущений; кеш RSI Launcher не очищається, поки лаунчер працює. Автозапуск системних, драйверних і невідомих компонентів не вимикається автоматично.

## Перевірка збірки

GitHub Actions:

- компілює .NET 8 Windows Forms застосунок;
- перевіряє, що результат — рівно один автономний EXE;
- запускає self-test ручного шляху Star Citizen та реального видалення тестового shader cache;
- запускає окремий self-test автозапуску: створює тестовий реєстровий пункт і файл, вимикає їх, перевіряє видалення, відновлює та перевіряє точний попередній стан;
- запускає застосунок і створює UI snapshot-перевірки для основних сторінок на 1040×700 та 1280×800, включно з «Автозапуск» і «NVIDIA / GPU».

Резервні копії: `%ProgramData%\TiHiY\SystemOptimizer\Backups`.
Резервні копії автозапуску: `%ProgramData%\TiHiY\SystemOptimizer\StartupBackups`.
Журнали очищення: `%ProgramData%\TiHiY\SystemOptimizer\CleanupLogs`.
